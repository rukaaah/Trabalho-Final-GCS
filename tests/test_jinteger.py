"""
Suíte de testes para a classe JInteger.

OBJETIVO:
Garantir que os ~40 métodos da classe wrapper Integer do Java SE 8 
tenham o comportamento replicado corretamente no Python.

O QUE TESTAR AQUI:
- Parsing e Formatação (ex: parseInt com radix, toString).
- Operações bit a bit (ex: bitCount, rotateLeft).
- Aritmética estática (ex: sum, max, min).
- Constantes (MAX_VALUE, MIN_VALUE, SIZE).

REGRA GCS ANTI-ATALHO:
Um Pull Request NÃO pode conter mais do que 7 casos de testes implementados.
Faça PRs curtos e frequentes!
Um commit não pode conter mais de 3 métodos de teste.
"""
# from javalang.jinteger import JInteger

def test_jinteger_parse_int():
    # TODO: Implementar teste
    # assert JInteger.parseInt("10") == 10
    pass